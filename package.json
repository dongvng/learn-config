{
  "name": "node-config",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:dev": "cross-env NODE_ENV=dev node config.js",
    "build:staging": "cross-env NODE_ENV=staging node config.js",
    "build:prod": "cross-env NODE_ENV=prod node config.js"
  },
  "keywords": [],
  "author": "Dong Nguyen",
  "license": "ISC",
  "dependencies": {
    "config": "^3.3.6",
    "cross-env": "^7.0.3",
    "js-yaml": "^4.1.0"
  },
  "devDependencies": {
    "dotenv": "^10.0.0"
  }
}
